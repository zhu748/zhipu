node_modules/
*.log
config.yaml
.omo/evidence/
dist/
.DS_Store
*.tsbuildinfo
/.omo
/_reverse
/zcode-proxy.exe
/zcode-proxy
# Compiled Windows binaries — never track these (103MB), produce via CI/release instead
/zcode-proxy-windows.exe
/zcode-proxy-*.exe
*.exe
*.zip
release/zcode-proxy.exe
zcode-proxy-debug-*.json
# v0.2.0.9+: header debug output dir — per-request JSON files with (masked) auth
# tokens. Never track these — they're debugging artifacts that may contain
# sensitive header values.
/header-debug/
# Render: local data dir (only relevant if you `docker run` with a bind mount)
/data/
